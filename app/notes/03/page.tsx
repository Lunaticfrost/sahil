import Link from "next/link";
export default function Note() { return <main className="article-page">
<nav className="nav"><Link className="brand" href="/">SAHIL</Link><Link className="back" href="/#writing">← WRITING</Link></nav>
<header className="article-head"><span className="section-kicker">WRITING / 03</span><h1>Retries are not reliability</h1><div className="article-meta">BACKEND · 8 MIN READ · SEPTEMBER 2026</div></header>
<article className="article-body"><p>A retry is easy to add. Knowing whether a retry is correct is much harder.</p><p>When a request fails, the first instinct is often to try again. Sometimes that is exactly right. Sometimes it makes the original problem worse.</p><p>A useful retry design starts with the failure itself. Is the operation idempotent? Is the failure transient? How many times should we retry? How quickly? What happens after the final attempt? Can another part of the system safely continue?</p><p>That leads to a broader point: reliability is about defining failure behavior, not pretending failure will not happen.</p><p>Retries, timeouts, backoff, queues, dead-letter paths, observability, and recovery mechanisms are all pieces of that design.</p><p>The goal is not a system that never fails. The goal is a system whose failures are bounded, visible, understandable, and recoverable.</p></article></main>; }