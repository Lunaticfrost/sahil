import Link from "next/link";
export default function Note() { return <main className="article-page">
<nav className="nav"><Link className="brand" href="/">SAHIL</Link><Link className="back" href="/#writing">← WRITING</Link></nav>
<header className="article-head"><span className="section-kicker">WRITING / 02</span><h1>The difference between writing code and owning a system</h1><div className="article-meta">ENGINEERING · 7 MIN READ · SEPTEMBER 2026</div></header>
<article className="article-body"><p>Writing code gives a system a capability. Owning it means accepting responsibility for what happens next.</p><p>A feature can be correct when it leaves your editor and still create problems in production. Deployments can fail. Dependencies can behave differently. A database can become the bottleneck. An alert can be misleading. Someone can get paged at two in the morning without knowing what to do.</p><p>That is why I think about engineering ownership as a larger loop: build → deploy → observe → operate → learn → improve.</p><p>Documentation is part of that loop. Runbooks are part of it. Monitoring is part of it. Incident response is part of it.</p><p>The code is still important. But the code is only one part of the system an engineer is responsible for.</p></article></main>; }